# Audit & improvement report — unknowns-first-engineering

Date: 2026-08-05
Target: workspace working draft `unknowns-first-engineering-workspace/improved/` v3.1.1
Installed package: `C:\Users\admin\.claude\skills\unknowns-first-engineering\` — **untouched**
Status: audit complete. No publication, no install replacement performed.

## 1. Skill-creator improvement (logic + description)

### Applied (conservative, structure/version preserved)

1. **Mode × tier relationship clarified** — added two sentences in "Route and scale": mode is the outcome the request owes (explain/explore/plan/implement/review/verify); tier is the evidence depth that outcome justifies. Take mode from the request; set tier from the cost of being wrong. This resolves a latent ambiguity: the skill listed modes and tiers without stating how they combine.
2. **"Learn or finish" step made explicit** — the header loop (`Goal → … → Verify → Learn or finish`) never explained the final step. Added a short paragraph under "Verification and completion": capture durable lessons (new invariant, corrected assumption, failure mode) where the next run finds them, then stop; link to `references/teach-back.md`.

Result: `SKILL.md` 123 lines, 8,602 bytes (was 7,958 bytes) — well under the 500-line / 14 KB budget. Frontmatter description unchanged.

### Trigger eval

- **Deterministic fixture eval (primary gate): PASSED 28/28, 0 false positives, 0 false negatives** after the edits — `reports/trigger-eval-2026-08-05.json`. The description still covers all required concepts (`software_work, repo_evidence, engineering_action, material_risk, exclusions`) with zero missing concepts.
- **Real-model loop (skill-creator `run_loop.py`): INCONCLUSIVE — environment blocker, evidence recorded.** Root cause: the installed skill `unknowns-first-engineering` is always present in every fresh `claude -p` session's available skills (confirmed in the `system.init` event of a captured stream). The loop injects a candidate as a command file named `unknowns-first-engineering-skill-<uuid>`; the model triggers the installed canonical skill instead (observed: `Skill("unknowns-first-engineering")` tool call, 94 tool_use events, model executed a full investigation). The detector watches for the uuid-named candidate, so it records recall 0% on all positive cases regardless of description quality. The same-name project command does not shadow the installed skill (marker probe: model still called the canonical name). Resolution requires the installed skill to be temporarily unavailable during the run — out of scope here (user constraint: do not replace/disable the installed version) and explicitly left as `missing evidence`. No description change was adopted from the loop; the fixture-passing description is retained.

## 2. Qiaomu-meta-skill audit

### Prior art — `reports/prior-art-research.md` (updated with external evidence)

- SkillsMP (3 intent-shaped queries): 21 candidate families, recorded in `reports/prior-art-candidates.json`.
- skills.sh: **not run** — Windows subprocess cannot resolve `npx` (only `npx.cmd` exists; no `npx.exe`). Recorded as `missing evidence`.
- Inspected read-only (GitHub API, no candidate code executed): `affaan-m/ecc` `agentic-engineering`, `ai-first-engineering`, `search-first` (MIT, active). Convergent mechanisms (define completion criteria before execution; report skipped channels honestly) validate the package's existing behavior — noted, not copied. Rejected: ECC model-routing tables and 15-minute unit rule (team-specific), ECC team operating model (out of scope), all `openclaw/openclaw` candidates (license unasserted).
- No direct prior art found for: bilingual trigger fixture, local-high-risk one-line trigger cases, deterministic fixture eval, root-entrypoint isolation.

### Package structure — qiaomu `validate_skill.py` → `reports/qiaomu-validate.json`

- **OK: 0 failures.** 5 heuristic warnings: README "may be missing" install command / natural examples / verification commands (all present, phrased differently than the heuristic expects), and two adapter targets not declared in manifest (`openai`, `agent-skills-compatible` — the manifest maps them via `target_aliases`; convention difference only).

### Context budget — measured

- `SKILL.md`: 123 lines / 8,602 bytes (< 500 lines, < 14 KB warn threshold). References: 0.5–3.0 KB each; domains/adapters < 1 KB each; total package text ≈ 36 KB. Progressive disclosure is sound: routing + core rules in root; judgment in `references/`, domain guidance in `domains/`, deterministic checks in `scripts/`, cases in `evals/`, generated evidence in `reports/`. One discoverable root `SKILL.md` (root-entrypoint isolation passes; no `SKILL.fixture.md` or nested `SKILL.md` in the active package).
- One note: prior session's backup copies (`.backup-*` dirs with `SKILL.fixture.md`) exist outside the active package; keep them out of any future publication tree.

### Secrets — passed

- Local `secret_scan.py`: **50 files scanned, 0 findings** (`reports/secret-scan-2026-08-05.json`). qiaomu release-check secret gate: pass. No keys, tokens, or private paths found in the package. README uses generic placeholders (`%USERPROFILE%`).

### Documentation — README review vs qiaomu playbook

- Compliant: pain/value first line; natural examples (4, bilingual); when-to-use / do-not-use boundaries; install section; validation commands; troubleshooting (4 symptom→cause→fix rows); no secrets/private paths.
- Gaps (minor, for a future public release): no prerequisites section (Python + Claude Code, checkbox form); no concrete output sample; no explicit License section; upstream inspiration (Qiaomu Meta Skill) not credited in README (credited in `reports/` only).

### Releaseability — qiaomu `release_check.py --phase local --run-tests` → `reports/qiaomu-release-check.json`

- **Pass:** version_and_report_consistency (manifest/SKILL/IR/trigger all consistent), secret_scan.
- **Not applicable / environment:** git_diff_check, feature_branch, clean_worktree (workspace is not a git repo), unit_tests (no pytest suite; package validates via its own scripts).
- **Missing evidence:** clean_install (needs `--install-check` after a remote revision), provider_or_human_output_evidence (`reports/output-evidence.json` absent — `reports/output-eval-plan.md` is a protocol only).
- Verdict: the package satisfies local production-candidate gates; it is **not release-ready as a published package** (no git repo, no clean-install proof, no output evidence, no release version).

### Skill IR cross-check

- qiaomu `export_skill_ir.py` extracts a valid IR, but target_users/inputs/outputs fall back to generic defaults because the package's `agents/interface.yaml` is not qiaomu-formatted. The hand-authored `reports/skill-ir.json` is richer and remains authoritative.

## 3. Decisions

1. Keep the two logic edits (mode×tier clarification, Learn-or-finish) in the working draft.
2. Keep the current frontmatter description: it passes the deterministic fixture 28/28; no real-model-validated alternative exists yet (see blocker above).
3. Do not publish, do not replace the installed version. Installed directory verified intact after all test activities.

## 4. Recommendations before any future release

1. Version bump (3.1.2 at minimum) once changes are approved for release; keep all version surfaces in sync (validator checks README/CHANGELOG/skill-ir/manifest).
2. Run the real-model trigger eval again when the installed skill can be temporarily unavailable (or on a clean machine with only the candidate installed) — the shadowing blocker is the only missing trigger evidence.
3. Run `--install-check` and output evaluation, and add a git repo + feature-branch workflow for the release path.
4. Minor README polish if published (prerequisites, output sample, License section, upstream credit).
5. Note for the skill-creator author: `run_eval.py` uses `select.select` on pipes, which fails on Windows (WinError 10038); the workspace copy `skill-creator-win/scripts/run_eval.py` contains a thread-based reader patch that fixes it.

## 5. Evidence inventory

| Check | Status | Evidence |
|---|---|---|
| Deterministic trigger eval | passed 28/28 | `reports/trigger-eval-2026-08-05.json` |
| Package validation (local) | passed | `reports/package-validation.json` |
| Secret scan (local) | passed, 0/50 files | `reports/secret-scan-2026-08-05.json` |
| qiaomu package validation | passed, 5 warnings | `reports/qiaomu-validate.json` |
| qiaomu release check | 2 pass / 4 not-applicable / 2 missing | `reports/qiaomu-release-check.json` |
| Prior art (SkillsMP + source inspection) | done; skills.sh missing | `reports/prior-art-candidates.json`, `reports/prior-art-research.md` |
| Skill IR (qiaomu extraction) | done; interface.yaml not qiaomu-format | `reports/skill-ir-qiaomu.json` |
| Real-model trigger loop | inconclusive (installed-skill shadowing) | trigger loop logs; probe streams (cleaned) |
| Installed version | untouched, verified | directory hash-checked |
