# Output Evaluation Plan

Status: **not run — missing evidence**

Use `evals/output_cases.json` with a human or model judge. Score each case against every rubric item and preserve the raw prompt, environment/tool access, response, score, and reviewer rationale.

A trigger pass does not prove output quality. Do not mark `manifest.json` output evidence passed until this protocol is actually executed against v3.1.1. Cross-host claims require separate Claude, Codex, and generic-agent runs.
